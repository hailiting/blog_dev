# Solana 签名演示项目

这是一个基于 React + TypeScript + Vite 的 Solana 消息签名演示项目。

## 功能特性

- 🔐 连接 Solana 钱包（支持 Phantom、Solflare、Torus 等）
- ⏰ 对当前时间进行消息签名
- 📝 显示签名结果和消息内容
- 🎨 现代化的 UI 设计

## 技术栈

- **React 18** - 前端框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **@solana/wallet-adapter** - Solana 钱包适配器
- **@solana/web3.js** - Solana JavaScript SDK

## 安装依赖

```bash
npm install
```

或使用 yarn:

```bash
yarn install
```

或使用 pnpm:

```bash
pnpm install
```

## 运行项目

```bash
npm run dev
```

然后在浏览器中打开 http://localhost:5173

## 使用说明

1. **安装钱包插件**：确保你的浏览器已安装 Phantom 或其他支持的 Solana 钱包插件
2. **连接钱包**：点击"Select Wallet"按钮，选择并连接你的钱包
3. **签名**：点击"签名当前时间"按钮，在钱包弹窗中确认签名
4. **查看结果**：签名成功后，页面会显示签名的消息和签名结果

## 项目结构

```
solana/
├── src/
│   ├── App.tsx          # 主应用组件
│   ├── App.css          # 应用样式
│   ├── main.tsx         # 应用入口
│   ├── index.css        # 全局样式
│   └── vite-env.d.ts    # TypeScript 类型声明
├── index.html           # HTML 模板
├── package.json         # 项目配置
├── tsconfig.json        # TypeScript 配置
├── vite.config.ts       # Vite 配置
└── README.md           # 项目说明
```

## 构建生产版本

```bash
npm run build
```

构建后的文件将输出到 `dist` 目录。

## 预览生产版本

```bash
npm run preview
```

## 注意事项

- 本项目使用 Solana Devnet（测试网络）
- 签名操作需要钱包插件支持
- 签名不会消耗任何 SOL 代币

## License

MIT

