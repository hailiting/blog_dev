# EIP-4844 (Proto-Danksharding)

EIP-4844 就是通过开辟一条临时的廉价“数据专用道”，大幅降低了layer2网络向以太坊主网提交数据的成本，从而让用户在二层网络的交易手续费变的极便宜

## 概述

**EIP-4844**，也称为 **Proto-Danksharding** 或 **Blob Transactions**，是以太坊在 2024 年 3 月 Dencun 升级中激活的重要提案。

### 🎯 核心目标

**大幅降低 Layer 2 (Rollup) 的数据发布成本**，使 L2 交易费用降低 **10-100 倍**。

### 📅 关键信息

- **提案编号**：EIP-4844
- **激活时间**：2024 年 3 月 13 日（Dencun 升级）
- **别名**：Proto-Danksharding、Blob Transactions
- **交易类型**：Type 3

## EIP-4844 的组成部分

### 1. Blob（Binary Large Object）

**Blob** 是一种新的数据类型，专门用于存储临时的、大量的数据。

#### Blob 特性：

- **单个 Blob 大小**：~128 KB (131,072 bytes)
- **每区块目标**：3 个 blobs
- **每区块最大**：6 个 blobs
- **存储时长**：约 18 天后删除（不永久存储）
- **数据验证**：使用 KZG 承诺，无需下载完整数据

```javascript
// Blob 参数
const BLOB_SIZE = 131072; // 128 KB
const FIELD_ELEMENTS_PER_BLOB = 4096; // 每个 blob 的字段元素数
const TARGET_BLOB_GAS_PER_BLOCK = 3 * BLOB_SIZE; // 393,216
const MAX_BLOB_GAS_PER_BLOCK = 6 * BLOB_SIZE; // 786,432
```

### 2. 新的交易类型：Type 3

```
Type 0: Legacy Transaction（传统交易）
Type 1: EIP-2930（访问列表交易）
Type 2: EIP-1559（动态费用交易）
Type 3: EIP-4844（Blob 交易）← 新增
```

#### Type 3 交易结构：

```javascript
const blobTransaction = {
  type: 3,

  // 常规字段（继承自 Type 2）
  to: '0x...',
  value: 0n,
  data: '0x...',
  gasLimit: 100000,
  maxFeePerGas: ethers.parseUnits('50', 'gwei'),
  maxPriorityFeePerGas: ethers.parseUnits('2', 'gwei'),
  nonce: 0,

  // Blob 特有字段
  maxFeePerBlobGas: ethers.parseUnits('10', 'gwei'), // blob gas 最高费用
  blobVersionedHashes: [
    '0x01...' // KZG 承诺的版本化哈希
  ],
  blobs: [blobData], // 实际 blob 数据（仅在发送时需要）
  commitments: [...], // KZG 承诺
  proofs: [...],      // KZG 证明
};
```

### 3. 独立的 Blob Gas 市场

EIP-4844 引入了**独立于主 gas 市场**的 blob gas 市场：

- **Blob Gas 价格**：独立的 blob base fee
- **定价机制**：类似 EIP-1559，但使用不同的调整函数
- **目标使用率**：每区块 3 个 blobs
- **弹性空间**：最多 6 个 blobs

#### Blob Base Fee 计算公式：

```javascript
function calculateBlobBaseFee(excessBlobGas) {
  const MIN_BLOB_BASE_FEE = 1n;
  const BLOB_BASE_FEE_UPDATE_FRACTION = 3338477n;

  return fakeExponential(MIN_BLOB_BASE_FEE, excessBlobGas, BLOB_BASE_FEE_UPDATE_FRACTION);
}

function fakeExponential(factor, numerator, denominator) {
  let output = 0n;
  let numeratorAccumulator = factor * denominator;
  let denominatorAccumulator = denominator;

  for (let i = 1n; numeratorAccumulator > 0n; i++) {
    output += numeratorAccumulator;
    numeratorAccumulator = (numeratorAccumulator * numerator) / (denominator * i);
  }

  return output / denominatorAccumulator;
}

// 使用示例
const excessBlobGas = 262144n; // 2 个 blob 的超额
const blobBaseFee = calculateBlobBaseFee(excessBlobGas);
console.log('Blob Base Fee:', ethers.formatUnits(blobBaseFee, 'gwei'), 'Gwei');
```

### 4. KZG 承诺（KZG Commitments）

**KZG** 是一种多项式承诺方案，用于验证 blob 数据的完整性。

#### 优势：

- ✅ 节点无需下载完整 blob 即可验证
- ✅ 承诺大小固定（48 字节）
- ✅ 验证速度快
- ✅ 支持数据可用性采样（DAS）

```javascript
// KZG 相关常量
const BYTES_PER_COMMITMENT = 48;
const BYTES_PER_PROOF = 48;
const BYTES_PER_FIELD_ELEMENT = 32;

// KZG 承诺生成（简化示意）
function generateKZGCommitment(blobData) {
  // 1. 将 blob 数据转换为多项式
  const polynomial = blobToPolynomial(blobData);

  // 2. 使用 KZG setup 的公共参数生成承诺
  const commitment = kzgCommit(polynomial);

  // 3. 生成证明
  const proof = kzgCreateProof(polynomial);

  return { commitment, proof };
}

// 验证 KZG 承诺
function verifyKZGCommitment(commitment, proof, blobHash) {
  return kzgVerifyProof(commitment, proof, blobHash);
}
```

## EIP-4844 vs EIP-1559 对比

| 特性           | EIP-1559          | EIP-4844               |
| -------------- | ----------------- | ---------------------- |
| **目标**       | 优化主网 gas 费用 | 降低 L2 数据成本       |
| **激活时间**   | 2021 年 8 月      | 2024 年 3 月           |
| **交易类型**   | Type 2            | Type 3                 |
| **数据存储**   | 永久存储在链上    | 临时存储（~18天）      |
| **费用市场**   | Gas market        | 独立的 Blob gas market |
| **主要受益者** | 所有以太坊用户    | Layer 2 用户           |
| **区块扩展**   | 15M-30M gas       | 额外 3-6 blobs         |
| **费用销毁**   | Base fee 销毁     | Blob base fee 销毁     |
| **数据验证**   | 执行所有交易      | KZG 承诺验证           |

## JavaScript 实现示例

### 1. 发送 Blob 交易（L2 Sequencer）

```javascript
const { ethers } = require('ethers');

async function sendBlobTransaction() {
  const provider = new ethers.JsonRpcProvider('https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY');
  const wallet = new ethers.Wallet('YOUR_PRIVATE_KEY', provider);

  // 准备 blob 数据（L2 批次交易数据）
  const batchData = prepareBatchData(); // 你的 L2 交易批次
  const blobData = encodeToBlobFormat(batchData); // 编码为 blob 格式

  // 生成 KZG 承诺
  const { commitment, proof, versionedHash } = generateKZGCommitment(blobData);

  // 获取当前费用数据
  const feeData = await provider.getFeeData();
  const block = await provider.getBlock('latest');

  // 计算 blob gas 费用
  const blobBaseFee = calculateBlobBaseFee(block.excessBlobGas || 0n);
  const maxFeePerBlobGas = blobBaseFee * 2n; // 设置为当前的 2 倍

  // 构建 Type 3 交易
  const tx = {
    to: '0xYourL2SequencerContract',
    data: encodeL2BatchSubmission(versionedHash), // 合约调用数据

    // 执行部分的 gas（使用 EIP-1559）
    gasLimit: 200000,
    maxFeePerGas: feeData.maxFeePerGas,
    maxPriorityFeePerGas: feeData.maxPriorityFeePerGas,

    // Blob 部分（新增）
    type: 3,
    maxFeePerBlobGas: maxFeePerBlobGas,
    blobVersionedHashes: [versionedHash],
    blobs: [blobData],
    commitments: [commitment],
    proofs: [proof],
  };

  console.log('📤 发送 Blob 交易');
  console.log('Blob 数量:', tx.blobs.length);
  console.log('Blob 大小:', blobData.length.toLocaleString(), 'bytes');
  console.log('Max Fee Per Blob Gas:', ethers.formatUnits(maxFeePerBlobGas, 'gwei'), 'Gwei');

  // 发送交易
  const txResponse = await wallet.sendTransaction(tx);
  console.log('交易哈希:', txResponse.hash);

  // 等待确认
  const receipt = await txResponse.wait();

  // 计算实际费用
  const executionCost = receipt.gasUsed * receipt.effectiveGasPrice;
  const blobCost = receipt.blobGasUsed * receipt.blobGasPrice;
  const totalCost = executionCost + blobCost;

  console.log('\n✅ 交易已确认');
  console.log('执行费用:', ethers.formatEther(executionCost), 'ETH');
  console.log('Blob 费用:', ethers.formatEther(blobCost), 'ETH');
  console.log('总费用:', ethers.formatEther(totalCost), 'ETH');

  return receipt;
}

// 辅助函数
function prepareBatchData() {
  // 准备 L2 交易批次数据
  return new Uint8Array(100000); // 示例：100KB 数据
}

function encodeToBlobFormat(data) {
  // 将数据编码为 blob 格式（128KB）
  const blobData = new Uint8Array(131072);
  blobData.set(data);
  return blobData;
}

function encodeL2BatchSubmission(blobHash) {
  // 编码合约调用数据
  const iface = new ethers.Interface(['function submitBatch(bytes32 blobHash)']);
  return iface.encodeFunctionData('submitBatch', [blobHash]);
}
```

### 2. 查询 Blob Gas 市场信息

```javascript
const { ethers } = require('ethers');

async function monitorBlobGasMarket() {
  const provider = new ethers.JsonRpcProvider('YOUR_RPC_URL');

  // 获取最新区块
  const block = await provider.getBlock('latest', true);

  console.log('📊 Blob Gas 市场状态');
  console.log('='.repeat(60));
  console.log('区块号:', block.number);
  console.log('时间:', new Date(block.timestamp * 1000).toLocaleString());
  console.log();

  // Blob 使用情况
  if (block.blobGasUsed !== undefined) {
    const TARGET = 3 * 131072;
    const MAX = 6 * 131072;
    const used = Number(block.blobGasUsed);
    const excess = Number(block.excessBlobGas || 0n);

    console.log('📦 Blob 使用情况:');
    console.log('  已使用:', used.toLocaleString(), 'blob gas');
    console.log('  目标值:', TARGET.toLocaleString(), 'blob gas (3 blobs)');
    console.log('  最大值:', MAX.toLocaleString(), 'blob gas (6 blobs)');
    console.log('  使用率:', ((used / TARGET) * 100).toFixed(2) + '%');
    console.log('  超额:', excess.toLocaleString(), 'blob gas');
    console.log();

    // 计算 blob base fee
    const blobBaseFee = calculateBlobBaseFee(BigInt(excess));
    const nextBlobBaseFee = calculateBlobBaseFee(BigInt(excess + Math.max(0, used - TARGET)));

    console.log('💰 Blob 费用:');
    console.log('  当前 Blob Base Fee:', ethers.formatUnits(blobBaseFee, 'gwei'), 'Gwei');
    console.log('  预计下一区块:', ethers.formatUnits(nextBlobBaseFee, 'gwei'), 'Gwei');
    console.log('  普通 Base Fee:', ethers.formatUnits(block.baseFeePerGas, 'gwei'), 'Gwei');
    console.log();

    // 费用对比
    const ratio = Number(blobBaseFee) / Number(block.baseFeePerGas);
    console.log('📊 费用比较:');
    console.log('  Blob Base Fee / Base Fee:', (ratio * 100).toFixed(2) + '%');

    // 计算发布 100KB 数据的成本
    const dataSize = 100000; // 100 KB

    // 方式 1: Calldata
    const calldataGas = dataSize * 16; // 16 gas per non-zero byte
    const calldataCost = BigInt(calldataGas) * block.baseFeePerGas;

    // 方式 2: Blob
    const blobsNeeded = 1; // 100KB < 128KB
    const blobGas = blobsNeeded * 131072;
    const blobCost = BigInt(blobGas) * blobBaseFee;

    console.log('\n💵 发布 100KB 数据成本:');
    console.log('  Calldata 方式:', ethers.formatEther(calldataCost), 'ETH');
    console.log('  Blob 方式:', ethers.formatEther(blobCost), 'ETH');
    console.log('  节省:', ((1 - Number(blobCost) / Number(calldataCost)) * 100).toFixed(2) + '%');
  } else {
    console.log('⚠️  该区块不包含 blob 数据');
  }

  // 统计区块中的 blob 交易
  let blobTxCount = 0;
  for (const txHash of block.transactions) {
    const tx = await provider.getTransaction(txHash);
    if (tx.type === 3) {
      blobTxCount++;
    }
  }

  console.log('\n📝 交易统计:');
  console.log('  总交易数:', block.transactions.length);
  console.log('  Blob 交易数:', blobTxCount);
  console.log('  Blob 交易占比:', ((blobTxCount / block.transactions.length) * 100).toFixed(2) + '%');
}

// 运行监控
monitorBlobGasMarket();

// 持续监控
setInterval(monitorBlobGasMarket, 12000); // 每 12 秒（一个区块）
```

### 3. 成本对比分析

```javascript
const { ethers } = require('ethers');

async function analyzeCostSavings() {
  const provider = new ethers.JsonRpcProvider('YOUR_RPC_URL');
  const block = await provider.getBlock('latest');

  // 假设参数
  const ETH_PRICE_USD = 3000; // ETH 价格
  const dataSizes = [50000, 100000, 200000, 500000]; // 不同数据大小

  console.log('💰 L2 数据发布成本对比分析');
  console.log('='.repeat(80));
  console.log(`ETH 价格: $${ETH_PRICE_USD}`);
  console.log(`Base Fee: ${ethers.formatUnits(block.baseFeePerGas, 'gwei')} Gwei`);

  // 计算 blob base fee
  const blobBaseFee = calculateBlobBaseFee(block.excessBlobGas || 0n);
  console.log(`Blob Base Fee: ${ethers.formatUnits(blobBaseFee, 'gwei')} Gwei`);
  console.log();

  console.log('数据大小 | Calldata 成本 | Blob 成本 | 节省金额 | 节省比例');
  console.log('-'.repeat(80));

  for (const size of dataSizes) {
    // Calldata 方式
    const calldataGas = size * 16;
    const calldataCostWei = BigInt(calldataGas) * block.baseFeePerGas;
    const calldataCostUSD = Number(ethers.formatEther(calldataCostWei)) * ETH_PRICE_USD;

    // Blob 方式
    const blobsNeeded = Math.ceil(size / 131072);
    const blobGas = blobsNeeded * 131072;
    const blobCostWei = BigInt(blobGas) * blobBaseFee;
    const blobCostUSD = Number(ethers.formatEther(blobCostWei)) * ETH_PRICE_USD;

    // 计算节省
    const savingsUSD = calldataCostUSD - blobCostUSD;
    const savingsPercent = ((savingsUSD / calldataCostUSD) * 100).toFixed(2);

    console.log(
      `${(size / 1000).toString().padEnd(9)}KB | ` +
        `$${calldataCostUSD.toFixed(2).padEnd(13)} | ` +
        `$${blobCostUSD.toFixed(2).padEnd(9)} | ` +
        `$${savingsUSD.toFixed(2).padEnd(9)} | ` +
        `${savingsPercent}%`
    );
  }

  console.log();

  // 年度成本分析
  console.log('📈 年度成本分析（假设每天发布 1000 个批次，每批次 100KB）');
  console.log('-'.repeat(80));

  const batchesPerDay = 1000;
  const daysPerYear = 365;
  const batchSize = 100000; // 100KB

  // Calldata 方式年度成本
  const calldataGasPerBatch = batchSize * 16;
  const calldataCostPerBatch = BigInt(calldataGasPerBatch) * block.baseFeePerGas;
  const calldataYearlyCost =
    Number(ethers.formatEther(calldataCostPerBatch)) * batchesPerDay * daysPerYear * ETH_PRICE_USD;

  // Blob 方式年度成本
  const blobGasPerBatch = 131072; // 1 blob
  const blobCostPerBatch = BigInt(blobGasPerBatch) * blobBaseFee;
  const blobYearlyCost = Number(ethers.formatEther(blobCostPerBatch)) * batchesPerDay * daysPerYear * ETH_PRICE_USD;

  console.log(`Calldata 年度成本: $${calldataYearlyCost.toLocaleString()}`);
  console.log(`Blob 年度成本: $${blobYearlyCost.toLocaleString()}`);
  console.log(`年度节省: $${(calldataYearlyCost - blobYearlyCost).toLocaleString()}`);
  console.log(`节省比例: ${(((calldataYearlyCost - blobYearlyCost) / calldataYearlyCost) * 100).toFixed(2)}%`);
}

analyzeCostSavings();
```

## 实际应用场景

### 1. Layer 2 Rollups

所有主流 L2 都已采用 EIP-4844 来降低成本：

```javascript
// Optimism 使用 Blob 发布批次
const optimismBatch = {
  sequencer: '0x...',
  batchIndex: 12345,
  transactions: [...], // L2 交易列表
  stateRoot: '0x...',
  blob: encodeBatchToBlob(transactions),
};

// Arbitrum 使用 Blob 发布数据
const arbitrumBatch = {
  sequencerAddress: '0x...',
  dataBlob: compressAndEncodeToBlob(txData),
  batchHash: '0x...',
};

// zkSync 使用 Blob 发布 zkProofs
const zkSyncBatch = {
  prover: '0x...',
  proof: zkProof,
  publicInputs: [...],
  blob: encodeProofToBlob(zkProof),
};
```

### 2. 主流 L2 对比

| L2 项目        | Blob 支持 | 平均节省 | 用途             |
| -------------- | --------- | -------- | ---------------- |
| **Optimism**   | ✅        | ~95%     | 交易批次数据     |
| **Arbitrum**   | ✅        | ~97%     | 压缩交易数据     |
| **Base**       | ✅        | ~95%     | Coinbase L2 批次 |
| **zkSync Era** | ✅        | ~90%     | zkProof + 交易   |
| **Scroll**     | ✅        | ~92%     | zkEVM 数据       |
| **Linea**      | ✅        | ~93%     | ConsenSys L2     |
| **Starknet**   | ✅        | ~88%     | Cairo 证明       |

### 3. 实际费用对比（2024 年数据）

```javascript
// 真实案例：Optimism 批次发布

// EIP-4844 之前（使用 Calldata）
const beforeEIP4844 = {
  date: '2024-02-01',
  avgBatchSize: 100000, // 100 KB
  avgGasPrice: 30, // Gwei
  calldataGas: 1600000,
  costPerBatch: '0.048 ETH',
  costUSD: '$120', // ETH = $2500
  batchesPerDay: 1000,
  dailyCost: '$120,000',
  monthlyCost: '$3,600,000',
};

// EIP-4844 之后（使用 Blob）
const afterEIP4844 = {
  date: '2024-04-01',
  avgBatchSize: 100000, // 100 KB
  avgBlobGasPrice: 3, // Gwei
  blobGas: 131072,
  costPerBatch: '0.0004 ETH',
  costUSD: '$1.20', // ETH = $3000
  batchesPerDay: 1000,
  dailyCost: '$1,200',
  monthlyCost: '$36,000',
};

console.log('💰 Optimism 成本节省:');
console.log('月度节省:', '$3,564,000');
console.log('节省比例:', '99%');
```

## 技术细节

### 1. Blob 数据格式

```javascript
// Blob 结构
const BLOB_TX_TYPE = 0x03;
const VERSIONED_HASH_VERSION_KZG = 0x01;
const BYTES_PER_FIELD_ELEMENT = 32;
const FIELD_ELEMENTS_PER_BLOB = 4096;
const BLOB_SIZE = BYTES_PER_FIELD_ELEMENT * FIELD_ELEMENTS_PER_BLOB; // 131072

// Blob 数据必须是有效的 BLS12-381 字段元素
function validateBlobData(blobData) {
  if (blobData.length !== BLOB_SIZE) {
    throw new Error('Invalid blob size');
  }

  // 每个 32 字节字段元素必须 < BLS_MODULUS
  const BLS_MODULUS = BigInt('52435875175126190479447740508185965837690552500527637822603658699938581184513');

  for (let i = 0; i < FIELD_ELEMENTS_PER_BLOB; i++) {
    const offset = i * BYTES_PER_FIELD_ELEMENT;
    const element = bytesToBigInt(blobData.slice(offset, offset + 32));

    if (element >= BLS_MODULUS) {
      throw new Error(`Invalid field element at index ${i}`);
    }
  }

  return true;
}
```

### 2. 版本化哈希

```javascript
// 生成版本化哈希
function computeVersionedHash(commitment) {
  const hash = sha256(commitment);
  hash[0] = VERSIONED_HASH_VERSION_KZG;
  return hash;
}

// 示例
const commitment = new Uint8Array(48); // KZG 承诺
const versionedHash = computeVersionedHash(commitment);
console.log('Versioned Hash:', ethers.hexlify(versionedHash));
// 输出: 0x01a7b3c4d5e6f7... (第一个字节是 0x01)
```

### 3. Gas 计算

```javascript
// Type 3 交易的总 gas 成本

// 1. 执行 gas（常规部分）
const executionGas = 21000 + calldataGas + executionGas;
const executionCost = executionGas * (baseFee + priorityFee);

// 2. Blob gas（独立计费）
const blobGas = blobCount * 131072;
const blobCost = blobGas * blobBaseFee;

// 3. 总成本
const totalCost = executionCost + blobCost;

console.log('执行成本:', ethers.formatEther(executionCost), 'ETH');
console.log('Blob 成本:', ethers.formatEther(blobCost), 'ETH');
console.log('总成本:', ethers.formatEther(totalCost), 'ETH');
```

### 4. 数据可用性保证

```javascript
// Blob 数据虽然只保存 18 天，但有多重保证机制

// 1. L2 节点保存完整数据
// 2. 以太坊归档节点可选择保存
// 3. 第三方数据可用性服务（如 EigenDA）
// 4. KZG 承诺永久保存在链上，可验证数据

// 数据可用性检查
async function checkBlobDataAvailability(blobHash, blockNumber) {
  const currentBlock = await provider.getBlockNumber();
  const blockAge = currentBlock - blockNumber;
  const daysOld = (blockAge * 12) / 86400; // 12秒一个区块

  if (daysOld > 18) {
    console.log('⚠️  Blob 数据可能已过期（超过 18 天）');
    console.log('建议从 L2 归档节点或 DA 服务获取');
  } else {
    console.log('✅ Blob 数据应该仍在以太坊网络中');
  }

  return daysOld <= 18;
}
```

## 与 Full Danksharding 的关系

EIP-4844 是 **Proto-Danksharding**（前置版本），为未来的 **Full Danksharding** 铺路。

### 区别对比：

| 特性          | Proto-Danksharding (EIP-4844) | Full Danksharding (未来) |
| ------------- | ----------------------------- | ------------------------ |
| **Blob 数量** | 3-6 个/区块                   | 64+ 个/区块              |
| **数据采样**  | 不支持                        | 支持 DAS                 |
| **验证方式**  | 全节点下载所有 blob           | 轻节点随机采样           |
| **吞吐量**    | ~0.375 MB/s                   | ~16 MB/s                 |
| **激活时间**  | 2024 年                       | 2025-2026 年（预计）     |

### 升级路径：

```
2021-08: EIP-1559 (优化 gas 费用)
         ↓
2024-03: EIP-4844 (降低 L2 成本 10-100 倍)
         ↓
2025-26: Full Danksharding (再降低 10-100 倍)
         ↓
未来:    完全分片系统
```

## 最佳实践

### 1. L2 Sequencer 建议

```javascript
// 批次打包策略
async function optimizeBatchPacking(transactions) {
  const MAX_BLOB_SIZE = 131072;
  const TARGET_BLOBS_PER_BLOCK = 3;

  // 1. 压缩交易数据
  const compressedData = compressTransactions(transactions);

  // 2. 分批
  const batches = [];
  for (let i = 0; i < compressedData.length; i += MAX_BLOB_SIZE) {
    batches.push(compressedData.slice(i, i + MAX_BLOB_SIZE));
  }

  // 3. 优化发送时机
  const blobBaseFee = await getCurrentBlobBaseFee();
  if (blobBaseFee < TARGET_BLOB_BASE_FEE) {
    // 费用低，立即发送
    await sendBlobBatches(batches);
  } else {
    // 费用高，等待或使用多个小批次
    await waitForLowerFee();
  }
}
```

### 2. 费用估算

```javascript
async function estimateBlobTransactionCost(dataSize) {
  const provider = new ethers.JsonRpcProvider('YOUR_RPC_URL');
  const block = await provider.getBlock('latest');

  // 计算需要的 blob 数量
  const blobsNeeded = Math.ceil(dataSize / 131072);

  // 执行 gas
  const executionGas = 200000; // 合约调用
  const executionCost = BigInt(executionGas) * block.baseFeePerGas;

  // Blob gas
  const blobGas = blobsNeeded * 131072;
  const blobBaseFee = calculateBlobBaseFee(block.excessBlobGas || 0n);
  const blobCost = BigInt(blobGas) * blobBaseFee;

  // 总成本
  const totalCost = executionCost + blobCost;

  return {
    blobsNeeded,
    executionCost: ethers.formatEther(executionCost),
    blobCost: ethers.formatEther(blobCost),
    totalCost: ethers.formatEther(totalCost),
    breakdown: {
      execution: ((Number(executionCost) / Number(totalCost)) * 100).toFixed(2) + '%',
      blob: ((Number(blobCost) / Number(totalCost)) * 100).toFixed(2) + '%',
    },
  };
}

// 使用
const estimate = await estimateBlobTransactionCost(100000);
console.log('预估成本:', estimate);
```

### 3. 监控和告警

```javascript
// 监控 blob gas 市场
async function setupBlobGasMonitoring() {
  const ALERT_THRESHOLD = ethers.parseUnits('50', 'gwei');

  setInterval(async () => {
    const block = await provider.getBlock('latest');
    const blobBaseFee = calculateBlobBaseFee(block.excessBlobGas || 0n);

    if (blobBaseFee > ALERT_THRESHOLD) {
      console.log('🚨 警告: Blob gas 费用过高!');
      console.log('当前:', ethers.formatUnits(blobBaseFee, 'gwei'), 'Gwei');
      // 发送告警...
    }

    // 记录指标
    logMetric('blob_base_fee', Number(blobBaseFee));
    logMetric('blob_usage', Number(block.blobGasUsed || 0n));
  }, 12000); // 每个区块
}
```

## 参考资源

- **官方规范**: [EIP-4844](https://eips.ethereum.org/EIPS/eip-4844)
- **KZG 仪式**: [ceremony.ethereum.org](https://ceremony.ethereum.org/)
- **Blob 浏览器**: [blobscan.com](https://blobscan.com/)
- **费用追踪**: [ultrasound.money](https://ultrasound.money/)
- **L2 数据**: [l2beat.com](https://l2beat.com/)

## 总结

EIP-4844 通过引入 **Blob** 这种临时数据存储方式，成功将 Layer 2 的数据发布成本降低了 **10-100 倍**，是以太坊扩容路线图上的重要里程碑。

### 关键要点：

- ✅ Blob 大小：128 KB
- ✅ 每区块 3-6 个 blobs
- ✅ 独立的 blob gas 市场
- ✅ 数据保存 ~18 天
- ✅ 使用 KZG 承诺验证
- ✅ 所有主流 L2 已采用
