# dart 字节

```dart
String a = "1";
String b = "1xxxxxxxxxxxxxxxxxx";
int c = 1;
int d = 9;

print(utf8.encode(a).length); // 字符串长度
print(utf8.encode(b).length);
print(c.bitLength); // int 长度
print(d.bitLength);
```
